import {BannerSlider} from "@/widgets/BannerSlider";

export const Main = () => {
       return (
           <section className={'w-full'}>
               <BannerSlider />
           </section>
       )
}