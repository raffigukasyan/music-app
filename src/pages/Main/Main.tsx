import {BannerSlider} from "@/widgets/BannerSlider";

export const Main = () => {
       return (
           <section className={'max-w-full'}>
               <BannerSlider />
           </section>
       )
}