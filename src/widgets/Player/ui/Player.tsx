import {BlockContainer} from "@/shared/ui/BlockContainer.tsx";

export const Player = ():JSX.Element => {
    return (
            <BlockContainer className={'[grid-column:_span_3/5]  py-7 w-full'}>
                PLAYER
            </BlockContainer>
    )
}